<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.member.dal.mysql.deviceuser.DeviceUserMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->


    <select id="selectDeviceUser" resultType="cn.iocoder.yudao.module.member.api.deviceuser.dto.DeviceUserDTO">
        SELECT nickname,
               mobile,
               user_id AS userId,
               device_id AS deviceId

        FROM `member_device_user` AS d
        JOIN `member_user` AS u
        ON d.user_id = u.id
        WHERE device_id IN
        <foreach collection="deviceIds" open="(" separator="," close=")" item="id">
            #{id}
        </foreach>
    </select>

    <select id="selectFamilyAndRoom" resultType="cn.iocoder.yudao.module.member.service.deviceuser.dto.FamilyAndRoomDeviceDTO">
        SELECT
            u.device_id AS deviceId,
            f.name AS family,
            r.name AS room
        FROM `member_family` AS f
                 JOIN `member_device_user` AS u
                      ON f.id = u.family_id
                 JOIN `member_room` AS r
                      ON r.id = u.room_id
        WHERE u.device_id IN
        <foreach collection="deviceIds" open="(" separator="," close=")" item="id">
            #{id}
        </foreach>
    </select>


</mapper>
